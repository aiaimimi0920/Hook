# Archived Design Specs

Files in this directory are retained as **historical design/spec documents**.

They are useful for understanding why a feature batch was designed a certain way, but they may diverge from the current implementation over time.

For current behavior, prefer the repository root docs and the live code.
