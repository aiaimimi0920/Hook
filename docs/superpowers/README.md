# Superpowers Archive

This directory keeps archived planning/spec material produced during earlier Hook development batches.

Subdirectories:

- `plans/`: archived execution plans
- `specs/`: archived design/spec docs

These files are useful as historical context, but they may not reflect the current repository state.
